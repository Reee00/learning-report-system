# Arsip Data Integrity Report

Laporan ini historis dan tidak boleh dipakai untuk menyimpulkan kondisi database aktif. Schema aktual dirujuk dari `database/migrations`; database aktif dan hasil migration deployment berstatus `Need Verification` tanpa `.env`/runtime evidence.
