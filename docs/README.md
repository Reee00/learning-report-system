# Dokumentasi Learning Report System

Dokumentasi ini disinkronkan dengan codebase pada 2026-09-03. Mulai dari [contextproject.md](../contextproject.md), lalu baca dokumen bernomor sesuai topik.

Sumber implementasi utama: `routes/web.php`, `app/Models`, `app/Services`, controller, migration, config, dan tests. Jika nilai environment tidak ada di repository, gunakan label `Need Verification`.

Role resmi: SuperAdmin, Relation, SPV Coach, Coach, PIC DK SCHOOL, TEACHER SCHOOL, Finance. `admin` hanya legacy migration/namespace kompatibilitas. Media baru local private storage; Cloudinary legacy compatibility saja.
